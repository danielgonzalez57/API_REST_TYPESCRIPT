import { Router, Request, Response } from "express";

const router = Router();

/** http://localhost:3002/auth/register [POST] */
router.get("/register", );
router.get("/login", );

export { router };